# private/

**Gitignored.** Nothing here is committed.

Holds references, not secrets: account identifiers, client contacts, private URLs, budgets,
owner answers. Prefer a pointer to the password manager over a copied value.

If this file is the only thing in the folder, that is fine — the folder exists so the
gitignore rule has something to protect.
